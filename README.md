# bggd
BGG data

Python 3.9
Django 3

## Install

Run the following in order to get scikit-surprise working on Windows
```python
python -m pip install --upgrade pip setuptools wheel
python -m pip install --no-use-pep517 scikit-surprise
```
